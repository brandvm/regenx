{
  "name": "regenx",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "dev": "node build.mjs --dev",
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "pnpm build && playwright test",
    "test:webflow": "pnpm build && node scripts/check-webflow.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "gsap": "3.15.0",
    "lenis": "1.3.24",
    "swiper": "11.2.10"
  }
}
